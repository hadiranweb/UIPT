[package]
name = "finance_lagoon"
version = "0.1.0"
edition = "2021"

[dependencies]
rts_core = { path = "../../core/rts_core" }
island_runtime = { path = "../../runtime" }
serde = { version = "1.0", features = ["derive"] }
